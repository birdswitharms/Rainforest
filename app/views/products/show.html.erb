<h1><%= @product.name %></h1>
<p><%= link_to 'edit', edit_product_path(@product.id) %> | <%= link_to("delete", product_path(@product.id), method: :delete, data: {confirm: "Are you sure you want to delete this product?"})  %>
<p>Description: <%= @product.description %></p>
<p>Price: <%= @product.price_in_dollars %></p>
<hr>

<h4>New Review</h4>
<%= render 'reviews/form', review: @reviews, product: @product %>


<h3>Reviews</h3>
<% @reviews.each do |review| %>
  <article>
    <p>Message: <%= review.message %></p>
    <p><%= review.created_at %></p> <%= link_to '(edit)', edit_product_review_path(review.id)  %>

  </article>
<% end %>
